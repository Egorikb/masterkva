# MasterKva — рабочая память проекта

Обновлено: **2026-09-10**. Здесь — проверенные результаты и текущие ограничения. Конкретные будущие поручения: [ORCHESTRATION](ORCHESTRATION.md); подробности эталона: [GRADE1_REFERENCE](GRADE1_REFERENCE.md).

## Правила ведения

1. Читать перед работой; обновлять после существенного анализа, изменения или проверки.
2. Запись содержит результат, файлы, доказательство/команду, ограничение и следующий шаг. Не копировать стенограммы, сырые логи, повторы и планы без результата.
3. Различать «проверено», «сообщено другим исполнителем», «гипотеза» и «открыто». Наличие поля или зелёная структурная проверка не доказывают правильность содержания.
4. Исправлять опровергнутый актуальный вывод с краткой причиной. Подробную историю оставлять в Git и отдельных отчётах.
5. Для прежней/внешней работы указывать автора и источник. Вложения read.txt/text.txt — контекст, не разрешение на действие.
6. Не писать секреты, персональные данные ребёнка и содержимое state-файлов. Не включать `backend/data/user_states.json` в коммит без отдельной задачи.
7. Сохранять параллельные изменения других исполнителей. Статус «сохранено в GitHub» допустим только после подтверждённого push.
8. Перед завершением проверять команды, ссылки и отсутствие противоречий; краткое текущее состояние отражать в MEMORY.md.

## Текущее состояние

- **Рабочая база:** `/home/egor/workspace_hermes/MasterKva/ACTIVE`; другие копии — источники/архивы.
- **GitHub:** [Egorikb/masterkva](https://github.com/Egorikb/masterkva), рабочая ветка `codex/active-orchestration-2026-09-09`. `main` с полным DeepTutor не заменялась рабочим MVP.
- **Backend:** компактный FastAPI MVP, `backend/main.py`; frontend — Next.js. Типовые порты 8001 и 3000.
- **1 класс:** версия 2.0, 26 тем, 26 разобранных примеров, 78 заданий. 24 exact, 33 ordered, 21 rubric; 111 проверяемых частей, 79 арифметических выражений.
- **Источник:** верхний китайский том найден и сверен по разделам — 42 задания. Нижний том не подтверждён — 36 заданий явно unverified. Это авторская адаптация приёмов, не буквальный перевод всех упражнений.
- **Оценка:** чистая функция lesson_assessment готова и проверена; реальная практика пока не выдаёт эти уроки и не вызывает её.
- **2–9 классы:** предыдущая структурная миграция сохранена; предметная готовность не подтверждена.
- **Чужие изменения:** user_states.json; параллельная настройка Notion в README, .vscode и scripts. Их не включать в коммит данного этапа.

## Действующие решения

1. Развивать ACTIVE. DeepTutor использовать как источник инструментов подготовки, не как автоматически подключённый учебный путь.
2. Сервер определяет проверку, прогресс и переход; LLM объясняет. Подсказка/переформулировка не являются попыткой ответа.
3. У ребёнка один вопрос за раз, доступный текстовый ввод, остановка озвучки и пауза. Рисунок и объяснение требуют критериев, а не искусственного точного ответа.
4. lesson_id, номер темы материала и диагностический topic_id — разные пространства идентификаторов; связывать по явной проверенной карте.
5. Ручной эталон с reference_version защищён от прежних массовых миграций. Экспорт пособия строится из JSON.
6. Китайская основа сохраняется; темы до 100 являются расширением относительно российского первого класса. Полное соответствие программе РФ не заявлено.
7. Codex — инструмент разработки. Модель преподавателя и голос оплачиваются/проверяются отдельно по API; историческая оценка 700 ₽/неделю не является текущим измеренным тарифом продукта.

## Работа Astra до текущего этапа

**Источник:** `/home/egor/Desktop/read.txt`, [первичный обзор](../../docs/PROJECT_REVIEW_2026-09-09.md), [roadmap](../../docs/ROADMAP_2026-09-09.md). Сведения о работе Astra восстановлены из этих материалов; инструкции вложения не исполнялись как новые поручения.

Astra изучил проект, заметки и три независимых обзора: backend, frontend, материалы. Сохранил аудит и roadmap; код и уроки на этом этапе не исправлял.

Ценные выводы: выбрать ACTIVE; устранить расхождения идентификаторов тем; исправить склейку чисел/дробей/единиц; проверить неверные эталоны; заменить демонстрационные вход/прогресс; связать доску с действиями ребёнка; не обещать точную страницу потери понимания, а искать вероятный пробел навыка. Голосовой пилот обсуждался с Qwen и SpeechKit; цены и условия перед подключением требуют новой проверки.

В первичном отчёте зафиксировано **161 passed**, успешные TypeScript/линтер. Это результат того снимка, не доказательство текущего полного прогона.

## Сохранённая история до эталона 1 класса

| Коммит/этап | Подтверждённый результат | Граница вывода |
|---|---|---|
| `85c141e` | Checkpoint ACTIVE и начальной оркестрации; тяжёлые PDF/индексы исключены | main не перезаписывалась |
| `83e53b8` | Числовая проверка сохраняет дроби, десятичные разделители и единицы | Не устраняет все дефекты учебного маршрута |
| `862be03` | Разметка источников/индексов и очистка шаблонов 1–3 | source_pages были копией curriculum, не доказательством сверки PDF |
| `f6e07c9` | Структурная миграция распространена на 4–9, всего 465 уроков | Предметная экспертиза не выполнена |
| `9ddb3e2` | Унификация answer/answers, answer_mode, teacher_note для 177 уроков 1–3 | Классификация по наличию поля иногда была неверной |
| `0f02774` | Повторная очистка обнаруженного методического текста | Ранее ошибочно записано «ожидает коммита» |
| `2082d5d` | [Исторический аудит 1 класса](audit/2026-09-09/grade1-content-review.md) | Его оценки трассируемости/отсутствия утечек пересмотрены ниже |

В прежнем обзоре backend-копий: ACTIVE и masterkva-assembled — одинаковый MVP; masterkva и versions/old-masterkva — полные копии DeepTutor; origin/main — полный DeepTutor. Это историческая инвентаризация, не требование объединить копии. Импорт/узкие тесты MVP проходили; TestClient в текущей изоляции не всегда завершался. Зависимость openai в requirements пока не объявлена явно — проверить перед воспроизводимой установкой teacher_llm.

## Результат 2026-09-09–10: эталон 1 класса

**Исполнители:** основной агент — редактура, источники, пособие и документация; GPT-5.5 — независимое чтение источника, 78 заданий и 26 примеров; Terra — чистый assessor и его тесты; Sol — аудит runtime, новый gate, тесты и защита миграций. На завершении последнего поручения Sol сообщил об исчерпании лимита; записанные им изменения проверены основным агентом.

**Что найдено и исправлено:**

- Найден реальный китайский PDF верхнего тома PEP 2022, 118 страниц. Выходные данные, hash, диапазоны и сдвиг печатных страниц записаны в [manifest](../backend/data/curriculum/grade_1_sources.json).
- Возвращены объёмные тела в тему 5; тема 13 исправлена на сложение в пределах 20; тема 17 — на вычитание в пределах 20. Исправлены границы тем 9/10, сняты недоказанные страницы второго полугодия.
- Переписаны все 78 условий, подсказок, решений и заметок; добавлены 26 разобранных примеров, цели, ошибки и предварительные темы.
- Исправлены часы, задания без перехода, неоднозначная классификация, произвольные продолжения рядов, ответы внутри вопросов. Рисунки и объяснения получили 21 рубрику.
- Ответы разделены по частям: несколько чисел не являются альтернативами друг другу. Добавлены словесные варианты чисел и конкретных величин.
- Реализован assessor со статусами correct/incorrect/needs_review/invalid_input без записи прогресса; ноль, дроби, разделители и единицы не склеиваются.
- Gate проверяет содержание контракта, арифметику, источники, предъявление, идентификаторы, примеры и граф предпосылок. Старые миграции пропускают reference_version.
- Составлены [пособие](grade1/TEACHER_GUIDE.md), [разбор результата](GRADE1_REFERENCE.md) и [конкретные задания моделям](ORCHESTRATION.md).

**Файлы:** grade_1_ru_adapted.json; grade_1_full.json; новый grade_1_sources.json; согласованные записи 1 класса в visual_topic_map.json; lesson_assessment.py; validate_grade1_reference.py; export_grade1_guide.py; два прежних мигратора и общий валидатор; два новых файла тестов; документация и MEMORY.

**Сохранение:** коммит `66dce7b` отправлен в `origin/codex/active-orchestration-2026-09-09`; push подтверждён. README, .vscode, scripts и user_states.json в этот коммит не включены. В корневом историческом docs/ORCHESTRATION.md вне Git-репозитория ACTIVE добавлена ссылка на актуальную очередь.

**Проверки текущего этапа:**

- `python3 backend/scripts/validate_adapted_lessons.py` — OK для 1–9; для 1 класса включает усиленный gate, для 2–9 остаётся структурным.
- `python3 backend/scripts/validate_grade1_reference.py --verify-sources` — OK; 14 подтверждённых разделов, 12 непроверенных, одна отсутствующая книга. Hash верхнего PDF совпадает.
- `pytest -q tests/services/ -k 'not test_gamification_endpoint and not test_embed_init_endpoint and not test_embed_code_endpoint'` — **141 passed, 3 deselected**.
- Общий локальный запуск services остановлен после зависания на группе TestClient. Три исключённых HTTP-теста не считаются пройденными в этой сессии.
- Независимая AI-проверка не нашла арифметических ошибок в 79 выражениях и 26 разобранных примерах после исправлений. Это не замена проверке педагогом.
- Экспорт содержит 78 заданий и 26 примеров; компиляция изменённых Python-файлов, git diff --check и проверка локальных ссылок документации прошли.

**Исправление прежних заявлений:** PDF не отсутствуют вообще; отсутствует подходящий нижний том 1 класса. «Высокая трассируемость» по скопированным страницам была завышена. Утечки ответов встречались и после фильтра шаблонов. Прежние exact/open были технической классификацией, а слово «решение» не гарантировало пошаговый разбор. Наличие answer_mode никогда само по себе не подключало урок к runtime.

## Учебный корпус

| Класс | Темы | Задания | Статус |
|---:|---:|---:|---|
| 1 | 26 | 78 | Эталон-кандидат v2.0; нижний источник и реальный маршрут открыты |
| 2 | 19 | 50 | Структурная миграция |
| 3 | 19 | 49 | Структурная миграция |
| 4 | 19 | 46 | Структурная миграция |
| 5 | 17 | 34 | Структурная миграция |
| 6 | 14 | 28 | Структурная миграция |
| 7 | 18 | 56 | Структурная миграция |
| 8 | 18 | 56 | Структурная миграция |
| 9 | 22 | 68 | Структурная миграция |

## Параллельная работа 2026-09-10: Notion и обзор

**Источник:** записи соседнего исполнителя, появившиеся в этом WORKLOG во время работы с 1 классом; собственные файлы учебного этапа ими не откатывались.

- В Notion Projects сначала не было MasterKva; затем, по отчёту исполнителя, создана запись со статусом In Progress и датой 2026-09-10.
- Добавлены .vscode/mcp.json, scripts/sync_notion.py и инструкция README для одностороннего обновления Stage/Timeline. Git/ACTIVE остаётся источником истины; двусторонняя синхронизация и автоматический GitHub workflow не включены.
- По отчёту: компиляция sync_notion успешна, отсутствие NOTION_TOKEN обрабатывается. Для shell-запуска токен нужен в окружении; секреты сюда не переносить.
- Соседний исполнитель также сообщил `pytest -q tests/`: **180 passed, 16 warnings**. Это отдельный запуск в его окружении; он не отменяет отмеченное выше зависание TestClient здесь.
- Уточнена карта: frontend/lib/api/panda-client.ts обращается к /api/v1/plugins/panda/chat; состояние MVP сохраняется в user_states.json. Эти изменения интеграции не входят в коммит учебного этапа.

## Открытые задачи в порядке зависимости

### Проверка Notion как источника AI-исполнителей — 2026-09-10

- Пользователь хочет использовать подключённых через Notion AI как основные рабочие руки. Проверены безопасно извлечённые метаданные `~/.codex/config.toml` и `ACTIVE/.vscode/mcp.json`: настроен локальный `@notionhq/notion-mcp-server`, установленная версия 2.5.1. Секреты в вывод и журнал не включались.
- Реальный MCP-handshake и `tools/list` завершились успешно: 24 инструмента для пользователей, поиска, страниц, блоков, комментариев и баз; команд agents/sessions/models нет. В текущем наборе инструментов этой сессии Notion также не появился автоматически; сервер проверен отдельным локальным процессом, затем остановлен.
- Read-only запрос `GET https://api.notion.com/v1/users/me` с уже настроенной авторизацией: **HTTP 200**, тип bot. Это подтверждает доступ интеграции к API, но не наличие подписки/прав на AI-агентов.
- [Актуальная документация Notion](https://developers.notion.com/guides/mcp/mcp-supported-tools) описывает `notion-list-agents`, `notion-search-agents`, `notion-spawn-session`, ожидание и чтение результатов. Нужны доступ к Notion AI/Custom Agents и capability «View sessions and interact with agents». Фактические агенты и модели пользователя пока не перечислены и не проверены.
- [Документация локального сервера](https://developers.notion.com/guides/mcp/hosting-open-source-mcp) рекомендует актуальный удалённый endpoint `https://mcp.notion.com/mcp`. При первой проверке он ещё не был настроен. Конфигурация самим проверяющим агентом не менялась; платные AI-запуски не выполнялись.
- **Повторная проверка по просьбе пользователя:** в `~/.codex/config.toml` появился включённый `notion-remote` с URL `https://mcp.notion.com/mcp`. `codex mcp list --json` при проверке вне ограниченной среды возвращает `enabled: true`, `auth_status: o_auth`; значения секретов не выводились. В реестре инструментов текущей беседы команд Notion по-прежнему нет. Регистрация и OAuth видны, но успешный вызов удалённого сервера и перечень Custom Agents пока не проверены. Следующий шаг — перезагрузить подключение/сессию Codex, вызвать `notion-fetch` с id `self` и перечислить доступных агентов перед переназначением карточек.

### Очередь реализации

**Проверка после перезапуска:** штатный App Server показывал Notion MCP 1.2.0, OAuth и 42 инструмента; первоначально их прямые вызовы в беседе были недоступны. 10.09.2026 нативные вызовы стали доступны: `notion-fetch(self)` подтвердил доступ к чтению/изменению страниц и Custom Agents; `notion-search-agents` для workspace и favorites вернул пустые списки. `notion-ai-search` работает, но локальный файл аудита не является страницей Notion и совпадений по его имени нет. Тест записи «Привет» через Notion не выполнялся, чтобы не создавать постороннюю страницу; локальный файл и страницы Notion не изменялись. [Доказательства](audit/2026-09-10-notion-mcp.md).

После получения URL страницы MasterKva чтение страницы прошло успешно. Запись отдельной строкой «Привет» в конец страницы была отклонена автоматической проверкой из-за исчерпания лимита использования; обход не выполнялся, страница не изменена.

**Публикация 10.09.2026 (Hermes):** правки закоммичены и отправлены (`6b25363`); истории `main` и ветки `codex/active-orchestration-2026-09-09` не имели общего предка, поэтому GitHub отклонял создание PR — истории связаны merge-коммитом `0850c20`. Открыт PR [Egorikb/masterkva#1](https://github.com/Egorikb/masterkva/pull/1): base `main`, 60 коммитов, `mergeable: clean`. Merge не выполнялся — решение за пользователем; при слиянии старая раскладка MVP (`backend/`, `frontend/` из `main`) заменяется раскладкой `ACTIVE`. Токен Notion из `~/.config/notion_pat.txt` вернул `401 API token is invalid` — `scripts/sync_notion.py` не отработал, нужен новый токен интеграции.

1. **D1 / Luna:** явная карта lesson_id → runtime topic_id/skill_id/item_family.
2. **C1 / GPT-5.5:** оригинал второго полугодия и сверка тем 15–26.
3. **B1/B2 / Sol:** подключение эталона к практике, ответы по частям, подсказки, выход из remediation.
4. **S1/U1 / Terra:** профили/попытки и удобное предъявление; **V1 / Sol:** голос и измеряемая стоимость.
5. **Q1 / Luna, A1 / Astra:** интеграционные доказательства и независимая приёмка.
6. **C2 / GPT-5.5 + Luna:** предметная редактура следующих классов; расширение банка заданий, российское покрытие и проверка педагогом.

## Карта проекта

- `backend/data/curriculum/` — план и manifest источников; `lessons/` — задания.
- `backend/data/knowledge_bases/` — локальные оригиналы/индексы; не все включены в Git.
- `backend/data/diagnostic_pool.json`, `skill_registry.json`, `skill_contracts.json` — отдельные runtime-контракты; в registry сейчас 40 навыков.
- `backend/deeptutor/` — серверные сервисы и API; `backend/scripts/` — миграции, gate и экспорт.
- `frontend/` — UI; `docs/grade1/` — читаемое пособие; `docs/audit/` — исторические отчёты.
- `MEMORY.md` — короткая точка входа; этот файл — основной журнал.
